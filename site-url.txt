USE THIS: 58ea74cccc6f1c17b6056aea





http://impulse-io.mtilhlenfield.c9users.io/

KyTrol: 58e9a7da243bf55bed7cd937

matthew: 58e9ad8261eca75fe4bfd40b


<div class="login shadow">
    
    <form [formGroup]="loginForm" (ngSubmit)="submit(loginForm.value)" class="center">
        <h3 class="capital center heading">Login</h3>
        <div class="error" *ngIf="errorMsg">
		    Unable to login: {{errorMsg}}
        </div>
        
        <div class="error border-box left"
			*ngIf="loginForm.get('username').hasError('required') && loginForm.get('username').touched">
			<span>Username is required.</span>
        </div>
        <input [formControl]="loginForm.controls.username" type="text" placeholder="Username" class="left" name="username"/>
        
    
        <div class="error border-box left"
			*ngIf="loginForm.get('password').hasError('required') && loginForm.get('password').touched">
			<span>Password is required.</span>
        </div>
        <input [formControl]="loginForm.controls.password" type="password" name="password" placeholder="Password" class="left"/>
        
        
        <button [disabled]="!loginForm.valid" type="submit" class="block-center capital shadow">Login</button>
        
        <p>Already have an account? Log in <a routerLink='/home/login'>here</a>.</p>
    </form>
</div>

http://stackoverflow.com/questions/35243443/pre-bootstrap-loading-screen-for-angular2

Impulse is a social networking website that we see as a small step toward a society in which a person's value and influence is not at all influenced by race, religion, gender, or sexual orientation and is instead determined entirely by a simple community rating system over which we have complete and unregulated control